[project]
name = "healthcore"
version = "0.1.0"
description = "HealthCore monorepo — Python scripts for data analysis and automation"
requires-python = ">=3.12"
dependencies = [
    "pandas>=3.0.5",
]

[tool.uv]
package = false
